perfect!
now, back to dev. 
using the embeddings, i need to create a question/answering system. 
that would be a filament screen, but not crud, so i need you to try to use filament ui/ux standards, to implemenet this. 
the option is in the left menu "Smart Search" that opens a smar search:
- there is box for search. i can also filter source, date range, department, category (seeing distinct values in the data)
- also i can select how many documents i want to see (by default 10, three options: 10,20,30)
- when i search, it uses embeddings search (pgvector) i see 2 elements:
 * a box that answers my question, using AI, and based on the text content of the (10) search results 
 * the list of the n results. the list resembles notices/list, try to mask the same look&feel as notices/list in filament.
try to implement this as standard as possible, from a ux perspective, using this filament components as much you can or know.
ask me questions if you need.
---

it looks great. can the option be after "Notices" in the menu?
---

question: do i have embeddings stored? (don't think so)
---

```
array:4 [
  "notices" => 367
  "with_embedding_vector" => 0
  "with_embedding_json" => 0
  "with_embedding_updated_at" => 0
] // vendor/psy/psysh/src/ExecutionClosure.php(41) : eval()'d code:1
```
so, we don't have embeddings.
i need: 
- a way to request embeddings and store (here in dev, and when i am in production)
- from now on, when a new notice is inserted (basically by the crawlers) react [asyncronously] request embeddings, and store them, so i don't need to do that manually.
that possible?

---

cool. now, vector search takes time, ai takes time, so i need some ui feedback.
can i see a spining wheel in the search button while working? also it should not be clickable while working

----


cool. now, some creativity.
we need to set the dashboard section.
i would like to see some main indicators (like how many notices, sources, journals), analysis runs, 
see some charts like daily activity etc, 
I let you design the dashboard so it looks cool and professional.

---

cool. let's now fix some stuff in the dasboard (my opinion)
- Regulatory library: only 3 widgets: sources, daily journals, notices (no embeddings, no analysis runs)
- Analysis outcomes cool piechart, but can it be same high as "Daily publication activity" -there is an empty space otherwise.
- latest notices nice, but too many cols, let's have date, source, title, url only (no ned for "view" because clicking goes view anyways)



